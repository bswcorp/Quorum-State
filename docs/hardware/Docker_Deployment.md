# 🐳 Panduan Deployment Universal (Docker)

Sistem ini menjamin **Quorum-State** dapat diinstal, di-upgrade, dan di-update di perangkat apa pun (Lenovo, IBM, Cloud) hanya dengan satu perintah.

### 1. Persiapan Perangkat (Install Docker)
Di mesin mana pun (Ubuntu/Debian/Windows):
```bash
sudo apt update && sudo apt install docker-compose -y
